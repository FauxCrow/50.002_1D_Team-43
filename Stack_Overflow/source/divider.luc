// divide a by b
// i guess try not to use this :(
module divider #(
    SIZE = 32 : SIZE > 0
)(
    input a[SIZE], // dividend
    input b[SIZE], // divisor
    output d[SIZE] // a/b
  ) {

  always {
    d = a/b;
  }
}